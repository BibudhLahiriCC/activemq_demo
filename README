The demo projects with Apache ActiveMQ, for later embedding in ETL. The plan
is to transfer data from operational database as XML files. The ETL server
would subscribe to the database server (slave), and then on receiving the changes,
would transform (T) and load (L) to the data warehouse schema.
